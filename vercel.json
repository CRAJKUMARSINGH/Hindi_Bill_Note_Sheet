{
  "buildCommand": "cd artifacts/hindi-bill && pnpm install && pnpm run build -- --config vite.config.vercel.ts",
  "outputDirectory": "artifacts/hindi-bill/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
