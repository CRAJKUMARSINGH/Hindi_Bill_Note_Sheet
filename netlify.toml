[build]
  base    = ""
  command = "npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/hindi-bill exec vite build --config vite.config.netlify.ts"
  publish = "artifacts/hindi-bill/dist"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
