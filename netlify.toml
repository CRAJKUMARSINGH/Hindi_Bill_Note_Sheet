[build]
  base    = "artifacts/hindi-bill"
  command = "cp package.netlify.json package.json && npm install && npm run build:netlify"
  publish = "dist"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
